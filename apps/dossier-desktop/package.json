{
  "name": "@dossier/dossier-desktop",
  "productName": "Dossier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/backend.js",
  "scripts": {
    "preflight:rust": "node ./scripts/check-rust.mjs",
    "preflight:port": "node ./scripts/check-port.mjs",
    "dev": "pnpm run preflight:rust && pnpm run preflight:port && pnpm run build:deps && pnpm run build && cross-env DOSSIER_UI_DEV_SERVER=http://localhost:5173 DOSSIER_BACKEND_SCRIPT=dist/backend.js tauri dev --config ./src-tauri/tauri.conf.json",
    "dev:desktop": "pnpm run preflight:rust && pnpm run preflight:port && pnpm run build:deps && pnpm run build && cross-env DOSSIER_UI_DEV_SERVER=http://localhost:5173 DOSSIER_BACKEND_SCRIPT=dist/backend.js tauri dev --config ./src-tauri/tauri.conf.json",
    "build:deps": "pnpm -C ../.. -r --filter @dossier/domain --filter @dossier/store --filter @dossier/local-server --filter @dossier/inference-engine --filter @dossier/connectors-google-takeout build",
    "build": "tsc -p tsconfig.json",
    "dist": "pnpm run preflight:rust && pnpm run build:deps && pnpm run build && tauri build --config ./src-tauri/tauri.conf.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@dossier/connectors-google-takeout": "workspace:*",
    "@dossier/inference-engine": "workspace:*",
    "@dossier/local-server": "workspace:*",
    "@dossier/store": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "latest",
    "@types/node": "^22.10.2",
    "cross-env": "^7.0.3",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
