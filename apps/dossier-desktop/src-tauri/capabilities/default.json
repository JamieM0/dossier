{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window local IPC capability set; no remote origins are granted capability access.",
  "local": true,
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
