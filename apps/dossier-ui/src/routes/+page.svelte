<section class="screen">
  <div class="placeholder">
    <h1>Dossier</h1>
    <p>The preference-based recommendation MVP is under construction.</p>
  </div>
</section>

<style>
  .screen {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100%;
    padding: var(--space-6);
  }

  .placeholder {
    max-width: 480px;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  h1 {
    font-family: var(--font-display);
    font-size: 2rem;
    color: var(--text-primary);
  }

  p {
    color: var(--text-secondary);
    font-family: var(--font-body);
    line-height: 1.6;
  }
</style>
