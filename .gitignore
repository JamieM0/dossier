# dependencies
node_modules/

# SvelteKit / Vite
.svelte-kit/
.vite/
dist/
build/

# generated updater manifest (built locally by version-bump.py)
/latest.json
.vercel/
.netlify/

# environment variables (keep an example file instead)
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# testing / coverage
coverage/
.playwright/
test-results/

# TypeScript build info
*.tsbuildinfo

# caches
.eslintcache
.stylelintcache

# misc
.DS_Store
Thumbs.db

# IDEs/editors
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp

# existing project ignores
design_docs/
.claude/
apps/dossier-ui/.svelte-kit/

design-docs/

# Catalogue builder: raw scrape outputs are huge and reproducible.
# We commit the converted runtime catalogue under apps/dossier-ui/static/
# but not the per-film raw scrape, the URL pool, or fetched HTML.
tools/catalogue-builder/data/
